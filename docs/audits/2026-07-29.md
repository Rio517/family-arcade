# Audit 2026-07-29 (baseline — commit f8e478b)

The founding report: a snapshot of known state so successive audits have
something to diff against. Compiled from the long build-out session rather
than a fresh full sweep — the next `/audit` run should verify every line.

Baseline: 6 games (Yahtzee logger + 5 registry games: Ship Battle, Chess,
Risk, Magic Coins, Rainbow Racer) · 297 tests passing · build OK with a
>500 kB chunk warning · tsc clean.

## Fixed during the session (spot checks, not a formal audit)

- Ship Battle: drag hit-testing fell into the 2px grid gaps → red preview
  flash. Replaced with geometry math (PR #42).
- Chess king-hunt variant lost its flag after the first move (caught by a
  new test before shipping).
- Galaxy 3D: dark-side ships had no contrast against the dark board (PR #43).
- Battleship preview harness rendered without the design system → misleading
  white screenshots. Harness now loads shared tokens.

## Open findings

- [MED] Risk / a11y: territory tap targets are tiny on phones (~6px for
  small territories). Known since the family playtest; needs a hit-area
  strategy (invisible enlarged hit paths or tap-disambiguation). (CONFIRMED)
- [MED] Build: main JS chunk exceeds 500 kB minified. Candidates: verify
  three.js is only in the lazy chunk; consider manualChunks for vendor
  splitting. (CONFIRMED — build warning)
- [LOW] Chess: profile points/wins only accrue from online games; local
  (hotseat) wins earn nothing. Family may consider that unfair — owner call,
  not a bug. (CONFIRMED by design)
- [LOW] Repo hygiene: stale remote branches accumulate because the sandbox
  cannot delete them (owner deletes via GitHub Branches page); the parked
  `claude/chess-themes-mockup` branch is superseded by the shipped theme
  system and can go.
- [LOW] New games (Magic Coins, Rainbow Racer) were added on main during the
  session and have not been through any AI review pass: test coverage,
  reduced-motion gating, and offline integrity unverified. (PLAUSIBLE — needs
  the next audit's sweep)

## Watchlist

- Bundle growth as games are added — recheck chunk sizes each audit.
- `docs/screenshots/` grows with every PR; prune shots no longer referenced.
- Two more games appeared in one week; keep the registry pattern honest
  (no game-specific code leaking into `src/app/` or `src/shared/`).
